/* Placeholder for the vendored Chart.js 4 UMD build.
 *
 * Drop the real minified file here to enable the dashboard price chart:
 *   curl -L https://cdn.jsdelivr.net/npm/chart.js@4/dist/chart.umd.min.js \
 *        -o frontend/src/chart.min.js
 *
 * Until then `window.Chart` stays undefined and dashboard.js renders a numeric fallback
 * instead of the chart — the app remains fully functional.
 */
